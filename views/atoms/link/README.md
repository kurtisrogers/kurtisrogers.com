# link

Semantic anchor with optional external target and aria-label.

**Props:** `href`, `label`, `external` (optional), `className` (optional), `ariaLabel` (optional)

**Usage:** `{% include "atoms/link/index.njk" with { href: "/", label: "Home" } %}`

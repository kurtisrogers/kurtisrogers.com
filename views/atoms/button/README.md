# button

Styled action link using Pico button classes.

**Props:** `href`, `label`, `variant` (optional), `external` (optional), `className` (optional)

**Usage:** `{% include "atoms/button/index.njk" with { href: "/contact", label: "Get in touch" } %}`

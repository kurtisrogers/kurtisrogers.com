# theme-toggle

Light/dark theme switcher; requires Alpine.js `theme` state on a parent element.

**Props:** none (uses Alpine `theme` and `toggle()`)

**Usage:** `{% include "atoms/theme-toggle/index.njk" %}`
